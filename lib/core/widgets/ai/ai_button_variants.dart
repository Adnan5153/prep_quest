/// Predefined variants for the [AiActionButton].
enum AiButtonVariant {
  filled,
  outlined,
  gradient,
  glass,
  elevated,
  minimal,
  floating,
  iconOnly,
}

/// Predefined sizes for the [AiActionButton].
enum AiButtonSize { small, medium, large }

/// Predefined states for the [AiActionButton].
enum AiButtonState {
  enabled,
  disabled,
  loading,
  processing,
  success,
  error,
  premiumLocked,
}

/// Predefined animation types for the [AiActionButton].
enum AiButtonAnimationType {
  none,
  scale,
  pulse,
  glow,
  ripple,
  fade,
  shimmer,
  breathing,
  gradientShift,
}
