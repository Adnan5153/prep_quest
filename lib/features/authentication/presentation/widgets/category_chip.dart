/// Re-export of the canonical `CategoryChip` so the auth screens can
/// reuse it without importing the global widget library everywhere.
library;

export '../../../../core/widgets/category_chip.dart' show CategoryChip;