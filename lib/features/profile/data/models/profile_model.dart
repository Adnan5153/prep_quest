/// Legacy alias kept so older imports still resolve.
///
/// The canonical DTO is now `user_profile_model.dart`.
library;

export 'user_profile_model.dart'
    show UserProfileModel, ProgressionModel, StudyStatsModel;