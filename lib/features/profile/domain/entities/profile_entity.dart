/// Legacy alias kept so older imports still resolve.
///
/// The canonical entity now lives in `user_profile.dart`. The same
/// value object was promoted to `UserProfile` to make the feature
/// surface self-explanatory.
library;

export 'user_profile.dart' show UserProfile;
