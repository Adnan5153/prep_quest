/// Forwarder — the canonical Profile screen lives at `profile_screen.dart`.
///
/// The detailed Edit Profile experience will land in a future phase.
/// Until then, the root route re-exports the canonical screen so
/// legacy imports resolve.
library;

export 'profile_screen.dart' show ProfileScreen;
