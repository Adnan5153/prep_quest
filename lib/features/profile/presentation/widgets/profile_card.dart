/// Legacy forwarder — generic card. Kept as a passthrough so older
/// imports resolve. The Profile screen composes dedicated cards
/// (avatar, header, stats, etc.) directly.
library;

export '../../../../core/widgets/glass_card.dart' show GlassCard;