enum MissionCardState { active, completed, locked }

enum MissionCardSize { standard, compact }

enum MissionCardRewardKind { xp, coin, gem, badge }

enum MissionCardTag { daily, weekly, monthly, premium, special, none }
