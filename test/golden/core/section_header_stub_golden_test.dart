import 'package:flutter_test/flutter_test.dart';

/// Golden tests for the [SectionHeader] widget stub.
///
/// NOTE: `lib/core/widgets/section_header.dart` is currently an empty
/// stub file. The production code that fulfils the "section header"
/// role lives in `lib/core/widgets/title_with_action.dart`. The
/// coverage for the section-header surface lives in
/// `section_header_golden_test.dart`. This file documents the stub
/// state so the test infrastructure keeps compiling.
void main() {
  group('SectionHeader stub (placeholder)', () {
    test('file exists in production code', () {
      expect(
        true,
        isTrue,
        reason: 'section_header.dart is currently a stub. Replace with a '
            'real SectionHeader widget export and remove this file. The '
            'section header surface is currently exercised via '
            'title_with_action.dart.',
      );
    });
  });
}