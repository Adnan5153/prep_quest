import 'package:flutter_test/flutter_test.dart';

/// Golden tests for the [GradientText] widget.
///
/// NOTE: `lib/core/widgets/gradient_text.dart` is currently an empty
/// stub file. These tests are placeholders for the eventual unified
/// `GradientText` widget.
///
/// Goldens targeted once the widget lands:
///   goldens/core/gradient_text_default_light.png
///   goldens/core/gradient_text_default_dark.png
///   goldens/core/gradient_text_violet_light.png
///   goldens/core/gradient_text_violet_dark.png
///   goldens/core/gradient_text_cyan_light.png
///   goldens/core/gradient_text_cyan_dark.png
void main() {
  group('GradientText (placeholder - widget not yet implemented)', () {
    test('file exists in production code', () {
      expect(
        true,
        isTrue,
        reason: 'gradient_text.dart is currently a stub. Implement the '
            'GradientText widget and replace this placeholder with real '
            'golden tests.',
      );
    });
  });
}